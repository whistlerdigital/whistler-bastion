# Contributing

Open an issue before a large change. Every policy rule must include deterministic safe and risky fixtures, a clear reason, and the least restrictive decision that safely represents the risk. Run `npm run check` before submitting a pull request.
